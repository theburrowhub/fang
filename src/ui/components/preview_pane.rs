/// Preview pane component stub.
pub struct PreviewPane;

impl PreviewPane {
    pub fn new() -> Self {
        Self
    }
}

impl Default for PreviewPane {
    fn default() -> Self {
        Self::new()
    }
}
